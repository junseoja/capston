# ============================================================
# Express 백엔드 환경변수 예시 파일 (.env.example)
# 이 파일을 복사해서 .env 로 이름을 바꾼 뒤 값을 채우세요.
# .env 파일은 .gitignore에 의해 Git에 올라가지 않습니다.
# ============================================================

# Express 서버 포트 (기본값: 3000)
PORT=3000

# FastAPI 서버 URL (기본값: http://localhost:8000)
PYTHON_API=http://localhost:8000

# Express → FastAPI 내부 호출 인증 키
# [추가 2026-05-10]
# 이유: FastAPI(8000)가 외부에 직접 노출되더라도, Express가 붙이는 내부 헤더 없이는
#      /completion, /feed, /user/password 같은 DB 변경 API를 호출하지 못하게 막기 위함.
# 설명: src/python_api/.env 의 INTERNAL_API_KEY 와 반드시 같은 긴 랜덤 문자열로 맞추세요.
INTERNAL_API_KEY=replace-with-a-long-random-shared-secret

# React 프론트엔드 URL - CORS 허용 대상 (기본값: http://localhost:5173)
FRONTEND_URL=http://localhost:5173

# ── 성능 측정 / 캐시 ───────────────────────────────────────────────────────
# [추가 2026-05-10]
# 이유: 구조(React→Express→FastAPI→MySQL)는 유지하면서 느린 API를 숫자로 찾고,
#      통계 API의 반복 조회 비용을 줄이기 위함.
SLOW_REQUEST_MS=500
STATS_CACHE_TTL_MS=60000

# ── AWS S3 (피드 이미지 업로드 저장소) ───────────────────────────────────────
# 2026-05-05 도입: 기존 src/backend/uploads/ 로컬 저장 → S3 객체 저장으로 전환
# 팀원 간 RDS 공유 시 "내 PC 이미지가 다른 팀원 PC에서 깨지는" 문제 해결.
# 자격증명은 IAM 사용자 routine-mate-s3-uploader 의 키
# (s3:PutObject + s3:DeleteObject 권한만 가진 최소 권한 사용자).

AWS_REGION=ap-northeast-2
AWS_S3_BUCKET=your-bucket-name
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
